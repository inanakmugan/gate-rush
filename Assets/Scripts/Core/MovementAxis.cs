namespace GateRush.Core
{
    /// <summary>
    /// Restricts which cardinal directions a block may move along (M7).
    /// </summary>
    public enum MovementAxis
    {
        Free,
        HorizontalOnly,
        VerticalOnly
    }
}
