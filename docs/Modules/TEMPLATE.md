# Module NN — `TypeName`

**Assembly:** `GateRush.X`
**Depends on:** Modules …
**Phase:** N.N

---

## Responsibility

One paragraph. What this module owns, and — just as usefully — what it does not.

---

## Public surface

Signatures only. No bodies. If you find yourself writing an implementation here,
it belongs in "Left to you" as a constraint instead.

---

## Design decisions (owner)

The decisions that are **not** negotiable, each with its reason. This section
carries the architecture. A decision without a reason will be re-litigated later;
a decision with one will not.

Cross-reference `DECISIONS.md` where the rationale is recorded in full.

---

## Left to you

Implementation detail deliberately unspecified — data structures, iteration
mechanics, allocation strategy, validation message wording.

If something appears here that actually matters to the architecture, it is in the
wrong section. Move it up.

---

## Tests

The behaviours that must hold, written as assertions about observable behaviour
rather than descriptions of the implementation.

Include at least one test that would fail if the module's central design decision
were quietly abandoned.
